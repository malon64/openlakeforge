version: "0.2"

metadata:
  project: "openlakeforge"
  owner: "sales"
  description: "Fixture contract using a forbidden shared Silver namespace."
  tags: ["sales", "fixture"]

storages:
  default: "lakehouse_bronze"
  definitions:
    - name: "lakehouse_bronze"
      type: "s3"
      bucket: "{{OPENLAKEFORGE_STORAGE_BRONZE_BUCKET}}"
      region: "{{OPENLAKEFORGE_STORAGE_REGION}}"
    - name: "lakehouse_silver"
      type: "s3"
      bucket: "{{OPENLAKEFORGE_STORAGE_SILVER_BUCKET}}"
      region: "{{OPENLAKEFORGE_STORAGE_REGION}}"

report:
  path: "floe/reports/sales/fixture"
  storage: "lakehouse_silver"

entities:
  - name: "orders"
    incremental_mode: "none"
    source:
      format: "csv"
      path: "sales/fixture/orders"
      storage: "lakehouse_bronze"
      options: { header: true, separator: ",", encoding: "utf-8", glob: "*.csv" }
      cast_mode: "strict"
    sink:
      write_mode: "overwrite"
      accepted:
        format: "iceberg"
        path: "sales/fixture/orders"
        storage: "lakehouse_silver"
        iceberg: { catalog: "iceberg_catalog", namespace: "silver", table: "orders" }
      rejected: { format: "csv", path: "floe/rejected/sales/fixture/orders", storage: "lakehouse_silver" }
    policy: { severity: "reject" }
    schema:
      normalize_columns: { enabled: true, strategy: "snake_case" }
      primary_key: ["order_id"]
      columns:
        - { name: "order_id", type: "string", nullable: false }
