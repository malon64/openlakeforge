version: "0.2"

metadata:
  project: "openlakeforge"
  owner: "supply-chain"
  description: "Inventory Reliability Bronze to Silver Floe contracts."
  tags: ["supply_chain", "inventory_reliability", "silver"]

storages:
  default: "lakehouse_bronze"
  definitions:
    - name: "lakehouse_bronze"
      type: "s3"
      bucket: "{{OPENLAKEFORGE_STORAGE_BRONZE_BUCKET}}"
      region: "{{OPENLAKEFORGE_STORAGE_REGION}}"
    - name: "lakehouse_silver"
      type: "s3"
      bucket: "{{OPENLAKEFORGE_STORAGE_SILVER_BUCKET}}"
      region: "{{OPENLAKEFORGE_STORAGE_REGION}}"
    - name: "openlakeforge_ops"
      type: "s3"
      bucket: "{{OPENLAKEFORGE_OPS_BUCKET_NAME}}"
      region: "{{OPENLAKEFORGE_STORAGE_REGION}}"

report:
  path: "floe/reports/supply_chain/inventory_reliability"
  storage: "openlakeforge_ops"

entities:
  - name: "warehouses"
    incremental_mode: "none"
    source:
      format: "csv"
      path: "supply_chain/inventory_reliability/warehouses"
      storage: "lakehouse_bronze"
      options: { header: true, separator: ",", encoding: "utf-8", glob: "*.csv" }
      cast_mode: "strict"
    sink:
      write_mode: "overwrite"
      accepted:
        format: "iceberg"
        path: "supply_chain/inventory_reliability/warehouses"
        storage: "lakehouse_silver"
        iceberg: { catalog: "iceberg_catalog", namespace: "supply_chain_inventory_reliability_silver", table: "warehouses" }
      rejected: { format: "csv", path: "floe/rejected/supply_chain/inventory_reliability/warehouses", storage: "lakehouse_silver" }
    policy: { severity: "reject" }
    schema:
      normalize_columns: { enabled: true, strategy: "snake_case" }
      primary_key: ["warehouse_id"]
      columns:
        - { name: "warehouse_id", type: "string", nullable: false }
        - { name: "warehouse_name", type: "string", nullable: false }
        - { name: "region", type: "string", nullable: false }
        - { name: "capacity_units", type: "integer", nullable: false }

  - name: "suppliers"
    incremental_mode: "none"
    source:
      format: "csv"
      path: "supply_chain/inventory_reliability/suppliers"
      storage: "lakehouse_bronze"
      options: { header: true, separator: ",", encoding: "utf-8", glob: "*.csv" }
      cast_mode: "strict"
    sink:
      write_mode: "overwrite"
      accepted:
        format: "iceberg"
        path: "supply_chain/inventory_reliability/suppliers"
        storage: "lakehouse_silver"
        iceberg: { catalog: "iceberg_catalog", namespace: "supply_chain_inventory_reliability_silver", table: "suppliers" }
      rejected: { format: "csv", path: "floe/rejected/supply_chain/inventory_reliability/suppliers", storage: "lakehouse_silver" }
    policy: { severity: "reject" }
    schema:
      normalize_columns: { enabled: true, strategy: "snake_case" }
      primary_key: ["supplier_id"]
      columns:
        - { name: "supplier_id", type: "string", nullable: false }
        - { name: "supplier_name", type: "string", nullable: false }
        - { name: "country", type: "string", nullable: false }
        - { name: "risk_tier", type: "string", nullable: false }

  - name: "inventory_snapshots"
    incremental_mode: "none"
    source:
      format: "csv"
      path: "supply_chain/inventory_reliability/inventory_snapshots"
      storage: "lakehouse_bronze"
      options: { header: true, separator: ",", encoding: "utf-8", glob: "*.csv" }
      cast_mode: "strict"
    sink:
      write_mode: "overwrite"
      accepted:
        format: "iceberg"
        path: "supply_chain/inventory_reliability/inventory_snapshots"
        storage: "lakehouse_silver"
        iceberg: { catalog: "iceberg_catalog", namespace: "supply_chain_inventory_reliability_silver", table: "inventory_snapshots" }
      rejected: { format: "csv", path: "floe/rejected/supply_chain/inventory_reliability/inventory_snapshots", storage: "lakehouse_silver" }
    policy: { severity: "reject" }
    schema:
      normalize_columns: { enabled: true, strategy: "snake_case" }
      primary_key: ["snapshot_id"]
      columns:
        - { name: "snapshot_id", type: "string", nullable: false }
        - { name: "snapshot_date", type: "date", nullable: false }
        - { name: "warehouse_id", type: "string", nullable: false }
        - { name: "product_id", type: "string", nullable: false }
        - { name: "on_hand_units", type: "integer", nullable: false }
        - { name: "allocated_units", type: "integer", nullable: false }
        - { name: "reorder_point", type: "integer", nullable: false }

  - name: "purchase_orders"
    incremental_mode: "none"
    source:
      format: "csv"
      path: "supply_chain/inventory_reliability/purchase_orders"
      storage: "lakehouse_bronze"
      options: { header: true, separator: ",", encoding: "utf-8", glob: "*.csv" }
      cast_mode: "strict"
    sink:
      write_mode: "overwrite"
      accepted:
        format: "iceberg"
        path: "supply_chain/inventory_reliability/purchase_orders"
        storage: "lakehouse_silver"
        iceberg: { catalog: "iceberg_catalog", namespace: "supply_chain_inventory_reliability_silver", table: "purchase_orders" }
      rejected: { format: "csv", path: "floe/rejected/supply_chain/inventory_reliability/purchase_orders", storage: "lakehouse_silver" }
    policy: { severity: "reject" }
    schema:
      normalize_columns: { enabled: true, strategy: "snake_case" }
      primary_key: ["purchase_order_id"]
      columns:
        - { name: "purchase_order_id", type: "string", nullable: false }
        - { name: "supplier_id", type: "string", nullable: false }
        - { name: "product_id", type: "string", nullable: false }
        - { name: "warehouse_id", type: "string", nullable: false }
        - { name: "order_date", type: "date", nullable: false }
        - { name: "expected_date", type: "date", nullable: false }
        - { name: "ordered_units", type: "integer", nullable: false }

  - name: "shipments"
    incremental_mode: "none"
    source:
      format: "csv"
      path: "supply_chain/inventory_reliability/shipments"
      storage: "lakehouse_bronze"
      options: { header: true, separator: ",", encoding: "utf-8", glob: "*.csv" }
      cast_mode: "strict"
    sink:
      write_mode: "overwrite"
      accepted:
        format: "iceberg"
        path: "supply_chain/inventory_reliability/shipments"
        storage: "lakehouse_silver"
        iceberg: { catalog: "iceberg_catalog", namespace: "supply_chain_inventory_reliability_silver", table: "shipments" }
      rejected: { format: "csv", path: "floe/rejected/supply_chain/inventory_reliability/shipments", storage: "lakehouse_silver" }
    policy: { severity: "reject" }
    schema:
      normalize_columns: { enabled: true, strategy: "snake_case" }
      primary_key: ["shipment_id"]
      columns:
        - { name: "shipment_id", type: "string", nullable: false }
        - { name: "purchase_order_id", type: "string", nullable: false }
        - { name: "ship_date", type: "date", nullable: false }
        - { name: "delivered_date", type: "date", nullable: false }
        - { name: "received_units", type: "integer", nullable: false }

  - name: "stockout_events"
    incremental_mode: "none"
    source:
      format: "csv"
      path: "supply_chain/inventory_reliability/stockout_events"
      storage: "lakehouse_bronze"
      options: { header: true, separator: ",", encoding: "utf-8", glob: "*.csv" }
      cast_mode: "strict"
    sink:
      write_mode: "overwrite"
      accepted:
        format: "iceberg"
        path: "supply_chain/inventory_reliability/stockout_events"
        storage: "lakehouse_silver"
        iceberg: { catalog: "iceberg_catalog", namespace: "supply_chain_inventory_reliability_silver", table: "stockout_events" }
      rejected: { format: "csv", path: "floe/rejected/supply_chain/inventory_reliability/stockout_events", storage: "lakehouse_silver" }
    policy: { severity: "reject" }
    schema:
      normalize_columns: { enabled: true, strategy: "snake_case" }
      primary_key: ["event_id"]
      columns:
        - { name: "event_id", type: "string", nullable: false }
        - { name: "event_date", type: "date", nullable: false }
        - { name: "warehouse_id", type: "string", nullable: false }
        - { name: "product_id", type: "string", nullable: false }
        - { name: "lost_units", type: "integer", nullable: false }
        - { name: "root_cause", type: "string", nullable: false }
