# Domains

`domains/` contains business and data-product logic.

Each domain owns its ingestion, Floe contracts, dbt models, Dagster assets, tests, and documentation.

The current seed domain is `sales`.
