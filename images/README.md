# Images

Custom runtime image definitions live here.

OpenLakeForge v1 uses two local custom images:

- `project-code`: Dagster domain code and pipeline runtime
- `superset`: Superset plus the Trino Python driver
