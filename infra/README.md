# Infrastructure

Infrastructure definitions live under `terraform/` and `helm/`.

Iteration 0 only establishes repository boundaries. Iteration 1 will introduce the local `k3d` foundation for namespaces, PostgreSQL, Garage, Polaris, and Trino.
